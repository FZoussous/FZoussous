public class method {
    public static void main(String [] args){
        //method = a block of reusable code that os executed whem called ()
        String name = "FZ";
        int AGE = 21;
            happyBirthday(name, AGE);// name and AGE are arguments

            //overloaded methods = methods that share the same name,
                                   //but diff parameters
                                   // signature = name + parameters
       
    }
    static void happyBirthday(String name, int AGE){  //here are considerd parameters
        System.out.println("Hey " + name + " " + AGE);
        
        }
    }

